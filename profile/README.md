# Wealthify

Платформа аналитики портфелей с ИИ‑подсказками. Этот README описывает организацию, стек, репозитории и процессы — актуализировано под текущие проекты и версии зависимостей.

---

## 🚀 Миссия
Дать каждому понятную и персонализированную аналитику активов (крипто, акции, облигации) без перегруза терминами.

## 🧩 Что мы делаем
- **Трекинг активов** (крипто/акции/облигации/кэш)
- **Риск‑профиль** и персональные объяснения на естественном языке
- **Алерты** по событиям и **дайджесты**

---

## 🛠️ Технологический стек (актуальный)
### Frontend — `wealthify-web`
- **Next.js 15.5.4** + **React 19.1.0** + **TypeScript 5.9.2**
- Формы: **react-hook-form 7.63** + **Zod 4.1** (резолверы `@hookform/resolvers 5.2`)
- Графики: **Recharts 3.2**
- Стили: CSS Modules / (Tailwind по проекту — опционально)
- Линтинг: **ESLint 9** (`eslint-config-next 15.5.4`)

### Backend — `wealthify-backend`
- **NestJS 11** (`@nestjs/* 11.x`), **TypeScript 5.7**
- БД: **PostgreSQL** + **Sequelize 6.37** / **sequelize‑typescript 2.1**, драйверы `pg` + `pg‑hstore`
- Валидация: **class‑validator 0.14** / **class‑transformer 0.5**
- Аутентификация: **passport** + **passport‑local**, **@nestjs/jwt**
- Документация: **@nestjs/swagger 11.2** + **swagger‑ui‑express 5.0**
- Утилиты: `cookie-parser`, `uuid`, `rxjs`
- Качество: **ESLint 9**, **Prettier 3**
- Тесты: **Jest 29** / **ts‑jest** / **supertest**

---

## 🏗️ Архитектура
```
Web (Next.js 15 / React 19)
  ├─ UI + Server/Client Components
  ├─ RHF + Zod формы
  └─ Recharts визуализации

API (NestJS 11)
  ├─ Auth JWT (access + refresh tokens)
  ├─ Доменные модули (портфели, алерты)
  └─ Swagger docs

Data Layer
  ├─ PostgreSQL
  └─ Sequelize (+ sequelize‑typescript)
```

---

## 📦 Репозитории
- **`wealthify-web`** — фронтенд (Next.js), App Router, формы, графики
- **`wealthify-backend`** — бэкенд (NestJS), auth, REST/Swagger, доступ к БД (Sequelize)

---

## 🌱 Быстрый старт
### 1) Frontend (`wealthify-client`)
**Требования:** Node.js LTS, pnpm/yarn

```bash
npm ci
npm run dev        # запустит next dev
# билд/прод
npm run build
npm run start
```
Окружение: создайте `.env.local` (см. `.env.example`, если есть).

### 2) Backend (`wealthify-backend`)
**Требования:** Node.js LTS, PostgreSQL 15+, (Docker — опционально)

```bash
npm ci
npm run start:dev  # Nest watch с NODE_ENV=development
# альтернатива
npm run start      # prod (без watch)
npm run build      # nest build
```
Окружение: `.env` с переменными подключения к БД, JWT‑секретом и т. п.

**База данных**
- Локально поднимите PostgreSQL (или через Docker Compose)
- Примените миграции/скрипты инициализации (если предусмотрены)
- Для инспекции удобно использовать **DBeaver**

**Документация API**
- Swagger включён. Точный путь смотрите в `main.ts` (в проекте `wealthify-backend`).

---

## 🧪 Проверки и скрипты
**Frontend**
```bash
npm run lint       # eslint
```

**Backend**
```bash
npm run lint        # eslint + prettier plugin
npm run test        # jest
npm run test:watch
npm run test:cov
```

---

## 📞 Контакты
- Tech Lead: `@outea7t`
